import 'package:flutter/material.dart';

class AppColors {
  // Primary — YatraGo blue
  static const Color primary = Color(0xFF185FA5);
  static const Color primaryLight = Color(0xFFE6F1FB);
  static const Color primaryDark = Color(0xFF0C447C);

  // Driver mode — coral
  static const Color driver = Color(0xFF993C1D);
  static const Color driverLight = Color(0xFFFAECE7);

  // Success — teal
  static const Color success = Color(0xFF0F6E56);
  static const Color successLight = Color(0xFFE1F5EE);

  // Warning — amber
  static const Color warning = Color(0xFF854F0B);
  static const Color warningLight = Color(0xFFFAEEDA);

  // Error — red
  static const Color error = Color(0xFFA32D2D);
  static const Color errorLight = Color(0xFFFCEBEB);

  // Neutrals
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF5F5E5A);
  static const Color textTertiary = Color(0xFF9C9A92);
  static const Color border = Color(0xFFDDDDDD);
  static const Color borderLight = Color(0xFFF1EFE8);
  static const Color background = Color(0xFFFAFAF8);
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF1A1A1A);

  // Star rating
  static const Color star = Color(0xFFF5A623);
}
